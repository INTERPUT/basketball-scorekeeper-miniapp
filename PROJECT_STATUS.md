# 篮球技术台自动化 - 项目状态

## 当前需求基线

- 产品文档正文版本：`v0.7`
- 事件字典正文版本：`v0.3`
- 页面流程正文版本：`v0.4`
- 技术方案正文版本：`v0.2`
- 说明：工作区现有文件名仍保留较早版本号，但正文内容已经升级并相互对齐；后续开发以正文版本为准。

## 已完成

- 2026-05-18：完整阅读四份需求文档。
- 2026-05-18：检查工作区，确认当前只有需求文档、原型资源和空的 `coding区`。
- 2026-05-18：修正文档问题：产品文档中的正式计分表图片路径改为 `./assets/暂用篮球协会数据统计表格.jpg`。
- 2026-05-18：完成阶段 0 的本地工程骨架：原生微信小程序目录、TypeScript、Vitest、基础 CI。
- 2026-05-18：完成阶段 1 的首轮实现：`Match` / `Room` / `Team` / `Player` 模型、建赛页、加入页、技术员落地页、只读比赛信息页、本地房间服务。
- 2026-05-18：完成阶段 2 的本地规则核心：事件回放、逐节比分、计时状态机、暂停额度、个人 5 犯提醒、球队第 5 犯提醒、最后 1 分钟提醒。
- 2026-05-18：完成阶段 3 的本地文本录入链路：固定草稿契约、文档样例解析、确认后写入正式事件流。
- 2026-05-18：完成阶段 4 的本地同步骨架：快照订阅总线、技术台确认后观众页同运行时内自动刷新。
- 2026-05-18：已补齐外部接入资料：微信小程序 `appid`、云开发 `envId`、火山引擎 ASR `appId` / `accessToken` / `secretKey` / `cluster`；其中敏感凭据保存在本地资料文件，不应写入前端源码。
- 2026-05-18：已把微信小程序真实 `appid` 写入 `coding区/project.config.json`。
- 2026-05-19：完成真实云开发接线：小程序初始化 `wx.cloud.init`，前端核心服务切换到云函数、云数据库和实时监听。
- 2026-05-19：完成云函数 `createMatch`、`parseEvent`、`confirmEvent`、`transcribeAudio`、`saveSignature`、`generateArchive` 并部署到 `cloudbase-d0gqn3qol9fab297a`。
- 2026-05-19：完成阶段 5 真实语音输入：火山 ASR 已切到与 `volcengine_input_common` 匹配的 V2 WebSocket 协议，ASR 凭据仅存在云函数环境变量。
- 2026-05-19：完成阶段 6 赛后归档：双方队长签字、归档版本、正式计分表 HTML、PDF 报告、结构化 JSON、房间归档后关闭。
- 2026-05-19：完成阶段 7 首轮稳定性收尾：集合权限配置、技术员写权限校验、归档后禁止普通事件录入、云端端到端回归。
- 2026-05-19：补齐归档页持久化状态：新增 `getArchiveStatus` 云函数，重新进入签字页可恢复已保存签字和最新归档。
- 2026-05-19：补齐导出文件可用性：归档文件返回临时链接，前端支持打开 PDF 报告、复制正式计分表 HTML / 结构化 JSON 链接。
- 2026-05-19：补齐语音异常兜底：录音权限拒绝、录音 API 报错、录音文件缺失时提示改用文本输入。
- 2026-05-19：清理本地临时 ASR WAV 文件，并在 `.gitignore` 忽略本地音频调试文件。
- 2026-05-19：补齐微信开发者工具运行入口：新增 `build:miniprogram`，并生成 `miniprogram/**/*.js`，避免 DevTools 只看到 `.ts` 入口；尝试过内置 TypeScript 编译插件，但最终改为直接加载生成后的 JS。
- 2026-05-20：根据模拟器反馈收敛加入页错误文案，隐藏云数据库 `document.get` 底层错误，不存在房间统一显示“未找到比赛房间。”。
- 2026-05-20：把建赛页球员名单从多行文本格式改为“添加球员”弹窗：分别输入球衣号码和球员名称，支持主客队添加、去重和删除。
- 2026-05-20：同步更新并重新部署 `createMatch` 云函数，避免云端校验继续返回“按号码 姓名录入”的旧格式提示。
- 2026-05-20：修复自然语言跨节流程：上一节结束后再次输入“开始计时”会进入下一节，也支持“开始下一节”；已同步部署 `parseEvent` 云函数。
- 2026-05-20：修复观众页初始空态：只读快照订阅现在先主动读取当前快照，再开启实时监听。
- 2026-05-20：把最终 PDF 报告改为中文输出，嵌入 Noto Sans SC 字体，并在球队总分之外加入双方球员个人得分与犯规统计；已重新部署 `generateArchive` 云函数。
- 2026-05-22：修复上传前置校验问题：`sitemap.json` 补齐合法 `rules` 规则。
- 2026-05-22：已通过微信开发者工具 CLI 上传开发版本 `0.1.0`，说明为“首版真实测试：自然语言录入、签字归档、中文PDF报告”。
- 2026-05-23：按三分钟演示脚本完成提交视频产物：生成 12 个演示画面、PowerPoint 视频源和 1080p MP4，覆盖痛点、建赛、观众加入、自然语言录入、结束、签字、归档导出和中文报告。
- 2026-05-23：补齐黑客松提交项“多维表格最终进度截图”：生成 `黑客松参赛区/最终提交材料/最终进度多维表格截图.png`。

## 进行中

- 首版功能闭环与黑客松提交材料已基本完成；当前剩余人工动作是上传/提交视频、确认体验版配置和最终检查提交入口。

## 下一步

1. 在微信公众平台后台进入版本管理，把已上传的开发版本 `0.1.0` 设置为体验版，并添加真实测试人员为体验成员。
2. 将 `黑客松参赛区/最终提交材料/篮球技术台自动化_三分钟演示视频.mp4` 上传到比赛要求的视频提交位置。
3. 按最终提交清单检查 README、Prompt 记录、三阶段进度文档和视频链接。
4. 根据真实手机录音表现确认 `wav` 录音格式兼容性；如目标机型不支持，再把前端录音改为 `pcm` 或增加云函数压缩音频转码策略。

## 阻塞项

- 当前没有外部凭据阻塞。
- 当前没有代码或凭据阻塞；真实测试发放需要微信公众平台后台把开发版本设为体验版并配置体验成员。
- PDF 产物当前是轻量赛后报告；完整中文正式计分表由 HTML 文件承载，可打印或另行转 PDF。

## 验证结果

- `npm run typecheck`：通过。
- `npm run build:miniprogram`：通过，已生成微信开发者工具可直接加载的 `app.js` 和页面 `index.js`。
- `npm test`：通过，当前共 6 个测试文件、21 个测试用例。
- 所有云函数 `node --check`：通过。
- 阶段 1 关键闭环已由测试覆盖：创建比赛后，可通过同一四位房间码重新读取只读比赛快照。
- 阶段 2 已由确定性模拟比赛测试覆盖：可不依赖 AI 完成比分、犯规、暂停、计时和提醒回放。
- 阶段 3 已由解析测试覆盖：事件字典中的核心文本样例可稳定生成草稿并确认入账。
- 阶段 4 已由订阅测试覆盖：快照变化会通知订阅者并刷新只读视图。
- 阶段 5 真实联调：云存储 WAV 录音通过 `transcribeAudio` 返回 `白队七号两分命中`；`fileID` 路径和签名 URL 路径均已验证。
- 阶段 6 真实联调：新房间 `2412` 从创建、解析、确认得分、比赛结束、双方签字到归档 v1 全链路通过，导出类型为 `score_sheet_html,archive_json,pdf_report`。
- 阶段 7 真实联调：归档后房间再次调用 `parseEvent` 被拒绝；集合权限已验证为 `rooms/snapshots=ADMINWRITE`，其余业务集合为 `ADMINONLY`。
- 导出文件抽检：归档 HTML 包含比分、双方队名、签字 base64 图片和比赛结束记录；PDF 文件头为 `%PDF-`。
- `getArchiveStatus` 云端抽检：已归档比赛可恢复 2 条签字、3 个导出文件，所有导出文件均带临时链接；PDF 临时链接下载后文件头为 `%PDF-`。
- 云函数权限抽检：当前函数权限为 `auth != null && auth.loginType != 'ANONYMOUS'`；云存储权限为 `PRIVATE`，文件读取通过云函数签发临时链接完成。
- 微信开发者工具局部自动化抽检：`islogin` 为 true，`auto` 端口开启成功；加入页真实输入不存在房间码 `2425` 后显示“未找到比赛房间。”。
- 微信开发者工具局部自动化抽检：建赛页真实点击“添加球员”，输入 `7` 和 `张三` 后，主队名单写入 `{ number: "7", name: "张三" }` 且弹窗关闭。
- 微信开发者工具云端冒烟：使用新名单结构创建测试房间 `4512` 成功，页面跳转到 `pages/scoreboard/index?roomCode=4512`；随后通过加入页进入同一房间成功，跳转到 `pages/match-info/index?roomCode=4512`。
- `createMatch` 云函数部署：通过，`filesCount=3`，`packSize=15.4 KB`。
- `parseEvent` 云函数部署：通过，`filesCount=3`，`packSize=15.8 KB`。
- 自然语言完整场景自动化：通过。测试房间 `2820`，录入 19 条文本事件，覆盖 4 节、得分、犯规、暂停、时间更正、比赛结束；最终比分 `8:6`，逐节比分 `1:[2,3] / 2:[3,1] / 3:[1,2] / 4:[2,0]`。
- 自然语言完整场景自动化：观众页加入和赛后只读查看通过，最终状态为 `finished`。
- 自然语言完整场景自动化：双方队长签字、生成归档 v1、导出 `score_sheet_html` / `archive_json` / `pdf_report`、打开 PDF 均通过。
- 最终 PDF 报告验证：`generateArchive` 重新部署通过，`filesCount=4`，`packSize=10.8 MB`；基于测试比赛 `2820` 重新生成归档 v2 并打开 PDF 成功。
- 最终 PDF 报告文本抽检：`pdftotext` 可提取中文标题“篮球比赛归档报告”、章节“球队得分 / 球员个人得分”，并包含张三 3 分、赵六 5 分、李四 5 分、王五 1 分等个人统计。
- 上传验证：`npm run build:miniprogram`、`npm run typecheck`、`npm test` 均通过后执行上传；首次上传因 `sitemap.json` 规则为空被微信校验拒绝，修复后重新上传成功。
- 微信上传结果：版本号 `0.1.0`，代码包总大小 `68.5 KB` / `70114` 字节；上传信息已写入 `coding区/.devtools/upload-info.json`。
- 演示视频录制前验证：`npm test` 通过，6 个测试文件、21 个测试用例；`npm run typecheck` 通过。
- 演示视频产物验证：`篮球技术台自动化_三分钟演示视频.mp4` 生成成功，MP4 容器包含正常 `ftyp/mdat/moov`，解析时长约 `172.29` 秒，满足 3 分钟以内要求。
- 最终提交复核：`npm run build:miniprogram`、`npm run typecheck`、`npm test` 均通过；远端 GitHub 仓库 `INTERPUT/basketball-scorekeeper-miniapp` 的 `main` 分支可访问。
